#!/bin/bash
sleep 30
INTERFACE_IP=`ifconfig <%= external_bridge_interface %> | awk '/inet addr/ {print $2}' | cut -f2 -d ":"`
ifconfig <%= external_bridge_interface %> 0
ifconfig <%= external_bridge_name %> ${INTERFACE_IP}
